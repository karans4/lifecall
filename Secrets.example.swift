import Foundation

// Copy this file to `Sources/Secrets.swift` and fill in your own keys.
// `Sources/Secrets.swift` is gitignored so real keys never get committed.
enum Secrets {
    static let vapiPrivateKey = "YOUR_VAPI_PRIVATE_KEY"   // Vapi → Dashboard → API Keys
    static let nebiusKey      = "YOUR_NEBIUS_API_KEY"     // Nebius Token Factory
    static let insforgeKey    = "YOUR_INSFORGE_API_KEY"   // Insforge project API key (ik_...)
}
